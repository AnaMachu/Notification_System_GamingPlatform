package com.notifications.model;

/**
 * Agrupa los tipos de evento en las dos categorías que el usuario
 * puede habilitar o deshabilitar de forma independiente.
 */
public enum NotificationCategory {
    GAME_EVENTS, SOCIAL_EVENTS
}
